name: DeployNewVersion

on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Build Artifacts
      run: ./builder.exe

    - name: Create Technic Zip File
      uses: thedoctor0/zip-release@master
      with:
        filename: 'technic.zip'
        directory: 'workdir/technic'

    - name: Create MultiMC Zip File
      uses: thedoctor0/zip-release@master
      with:
        filename: 'multimc.zip'
        directory: 'workdir/multimc'

    - name: List Files
      run: ls

    - name: Upload Technic Release Version
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.TOKEN }}
        file: technic.zip
        asset_name: technic.zip
        tag: pack
        overwrite: true

    - name: Upload MultiMC Release Version
      uses: svenstaro/upload-release-action@v2
      with:
        repo_token: ${{ secrets.TOKEN }}
        file: multimc.zip
        asset_name: multimc.zip
        tag: pack
        overwrite: true

    - name: Send message to discord
      uses: Sniddl/discord-commits@v1.3
      with:
        webhook: ${{ secrets.DISCORD_WEBHOOK }}
        message: " "
        embed: '{"title": "**Atualização**","description": "{{ env.DISCORD_MESSAGE }}","author": {"name": "{{ env.DISCORD_NAME }}", "icon_url": "{{ env.DISCORD_AVATAR }}"}}'